<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">
    <changeSet id="1" author="srk">
        <sql>
            CREATE TABLE course (
            id BIGINT NOT NULL AUTO_INCREMENT,
            title VARCHAR(255) NOT NULL,
            author VARCHAR(255) NOT NULL,
            category VARCHAR(255),
            publish_date DATE,
            CONSTRAINT pk_id PRIMARY KEY (id)
            );
        </sql>
        <rollback>
            DROP TABLE course;
        </rollback>
    </changeSet>
    <changeSet id="2" author="srk">
        <sql>
            CREATE TABLE transaction (
            id BIGINT NOT NULL AUTO_INCREMENT,
            course_id BIGINT NOT NULL,
            user_id BIGINT NOT NULL,
            date_of_issue DATETIME,
            CONSTRAINT pk_id PRIMARY KEY (id),
            CONSTRAINT fk_trans_course FOREIGN KEY (course_id) REFERENCES course(id) ON DELETE CASCADE ON UPDATE CASCADE
            );
        </sql>
        <rollback>
            DROP TABLE transaction;
        </rollback>
    </changeSet>
    <changeSet id="3" author="srk">
        <sql>
            INSERT INTO course(title, author, category, publish_date) VALUES ('Microservices', 'Instructor 1', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('Web Development', 'Instructor 2', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('JAVA', 'Instructor 3', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('Full Stack Development', 'Instructor 4', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('React JS', 'Instructor 5', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('AWS', 'Instructor 6', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('Android Development', 'Instructor 7', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('JSLT', 'Instructor 8', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('Hibernate', 'Instructor 9', 'Programming', NOW());
            INSERT INTO course(title, author, category, publish_date) VALUES ('Spring Security', 'Instructor 10', 'Programming', NOW());
        </sql>
        <rollback>
            TRUNCATE TABLE course;
        </rollback>
    </changeSet>
</databaseChangeLog>
